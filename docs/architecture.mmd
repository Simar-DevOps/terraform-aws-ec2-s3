flowchart LR
  dev[Developer] -->|opens PR| gh[GitHub Repo]
  gh -->|CI triggers| gha[GitHub Actions]
  gha --> fmt[terraform fmt]
  fmt --> validate[terraform validate]
  validate --> plan[terraform plan (no apply)]
  plan --> summary[Job Summary shows plan]
  summary --> reviewers[Review & approve]
  reviewers -->|merge to main| main[(main branch)]

  %% later:
  main -->|env: production (protected)| apply[terraform apply (gated)]
  apply --> cloud[(Cloud resources)]
